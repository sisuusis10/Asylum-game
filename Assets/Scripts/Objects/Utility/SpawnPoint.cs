using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPoint : MonoBehaviour {
/*
    public int PlaceID;
    private bool DestroyThis;

    int asf;
     
    private void Update() {
        if(PlaceID == SceneHandler.scene.Player_SpawnPoint) {
            PlayerController.p.transform.position = this.transform.position + new Vector3(0, 1.1f, 0);
            if(BlackOverlay.set.BlackTransition_Alpha < 0.9f) {
                DestroyThis = true;
            }
        } else {
            DestroyThis = true;
        }
        if(DestroyThis) {
            Destroy(this.gameObject);
        }
    }
    private void OnDrawGizmos() {
        Gizmos.DrawCube(this.transform.position+new Vector3(0,1.1f,0), new Vector3(1,2,1));
    }
*/
}
